# 基于Wayland的Linux录屏软件技术文档（C\+\+/Boost/Qt/FFmpeg）

# 1\. 项目概述

本项目旨在Linux Wayland桌面环境下开发一款轻量级、高性能的桌面录屏软件，区别于传统X11录屏工具，原生适配Wayland协议规范，解决Wayland环境下屏幕捕获权限受限、画面卡顿、兼容性差等问题。软件基于C\+\+核心开发，依托Qt实现可视化交互界面、Boost实现高性能异步任务调度、FFmpeg完成视频编码、封装与格式输出，实现全屏录制、区域录制、帧率可调、画质自定义、音频同步录制、多格式导出等核心功能。

项目核心优势为原生适配Wayland协议、低系统资源占用、异步无阻塞录制、编码格式丰富，适配主流Linux发行版（Ubuntu、Fedora、Debian等）的Wayland桌面会话。

# 2\. 需求分析

## 2\.1 功能需求

### 2\.1\.1 核心录制功能

- **屏幕捕获**：原生支持Wayland协议，实现全屏录制、自定义矩形区域录制，支持多显示器分屏录制，适配不同分辨率、刷新率的Wayland桌面窗口。

- **音频录制**：同步捕获系统音频、麦克风音频，支持单音频源、双音频源混合录制，兼容PulseAudio音频框架。

- **参数自定义**：支持自定义录制帧率（15/30/60fps）、视频画质、码率、编码格式，支持预设高清、标清、极速三种录制模式。

- **实时控制**：支持录制开始、暂停、继续、终止操作，实时显示录制时长、文件大小、帧率状态。

### 2\.1\.2 编码与导出功能

- 基于FFmpeg实现视频编码，支持H\.264、H\.265、VP9主流编码格式。

- 支持MP4、MKV、WebM、GIF多种文件封装格式导出。

- 支持录制完成后自动转码、压缩，适配不同播放设备。

### 2\.1\.3 界面交互功能

- 基于Qt实现轻量化图形界面，提供录制参数设置、区域选择、状态展示、文件保存路径配置功能。

- 支持全局快捷键控制录制启停、暂停，适配Linux窗口管理器。

- 录制完成后弹窗预览、打开文件目录、重新录制等快捷操作。

### 2\.1\.4 辅助功能

- 后台静默录制，最小化托盘运行，不占用桌面前台资源。

- 自动检测系统环境，判断当前桌面会话为Wayland/X11，适配不同环境。

- 日志记录功能，记录录制状态、异常报错、性能参数，便于问题排查。

## 2\.2 非功能需求

### 2\.2\.1 性能需求

- 低资源占用：录制过程CPU占用率≤15%，内存占用≤200MB，避免影响系统正常运行。

- 高稳定性：支持长时间连续录制，无画面撕裂、音画不同步、录制闪退问题。

- 实时性：屏幕帧捕获延迟≤20ms，音画同步误差≤10ms。

### 2\.2\.2 兼容性需求

- 系统兼容：适配Linux主流发行版，支持Wayland标准协议（wl\_shm、xdg\-desktop\-portal）。

- 硬件兼容：支持Intel、AMD、NVIDIA显卡，兼容硬件加速编码。

- 依赖兼容：适配主流版本Qt5/Qt6、Boost1\.70\+、FFmpeg4\.0\+。

### 2\.2\.3 可靠性需求

- 异常容错：支持磁盘空间不足、权限不足、编码失败等异常场景捕获与提示，避免程序崩溃。

- 数据安全：录制过程实时写入文件，意外退出可保留已录制片段，无数据丢失。

### 2\.2\.4 可扩展性需求

- 模块化架构设计，支持后续扩展截图、录屏剪辑、水印添加、直播推流功能。

- 统一接口封装，可快速适配新增编码格式、音频源、录制模式。

# 3\. 技术栈选型说明

|技术组件|版本要求|核心作用|
|---|---|---|
|C\+\+17|C\+\+17及以上|核心开发语言，保证高性能、低开销，支持多线程、异步编程，适配底层系统调用|
|Boost|1\.70\+|提供线程池、异步IO、定时器、信号处理能力，管理录制异步任务，保障任务调度稳定性|
|Qt|Qt5\.15/Qt6|搭建跨平台GUI界面，实现用户交互、区域选择、快捷键、托盘功能，提供基础事件循环|
|FFmpeg|4\.0\+|实现视频帧编码、音频采样处理、媒体封装、格式转换、硬件加速编码|
|Wayland Protocol|标准协议|调用xdg\-desktop\-portal、wl\_shm接口，实现Wayland环境下无权限绕过的合规屏幕捕获|
|PulseAudio|系统默认|Linux音频捕获接口，实现系统音频与麦克风音频采集|

# 4\. 系统技术架构

本软件采用**分层模块化架构**，自上而下分为UI交互层、业务逻辑层、核心能力层、系统适配层，各层解耦独立，通过统一接口调用，保证代码可维护性与扩展性。整体采用多线程异步架构，UI线程、帧捕获线程、编码线程、文件写入线程相互隔离，避免界面卡顿与录制阻塞。

## 4\.1 整体架构分层

### 4\.1\.1 UI交互层（Qt实现）

作为用户交互入口，基于Qt Widgets实现可视化界面，包含参数配置面板、录制控制按钮、区域选择组件、状态提示栏、系统托盘模块。独立UI线程，仅负责界面渲染、事件响应、参数传递，不参与耗时录制、编码操作，保证界面流畅性。

### 4\.1\.2 业务逻辑层（C\+\+\+Boost实现）

核心调度层，基于Boost线程池、异步定时器管理全局任务，负责接收UI层指令，调度底层捕获、编码、存储任务。包含录制状态管理、参数校验、任务调度、异常处理、音画同步调度五大核心模块，是连接UI层与核心能力层的中枢。

### 4\.1\.3 核心能力层（FFmpeg\+Wayland实现）

软件功能核心，包含屏幕帧捕获模块、音频采集模块、视频编码模块、媒体封装模块、文件存储模块。通过Wayland标准接口获取屏幕原始帧数据，通过PulseAudio获取音频采样数据，依托FFmpeg完成音视频数据编码、压缩、封装，输出标准媒体文件。

### 4\.1\.4 系统适配层（Linux系统调用）

底层系统适配，兼容Wayland/X11双环境，处理系统权限申请、桌面会话检测、硬件加速适配、系统资源监控，屏蔽不同Linux发行版、显卡硬件的差异化差异，为上层提供统一调用接口。

## 4\.2 核心模块交互流程

1\. 用户通过Qt UI配置录制参数、发起录制指令；2\. 业务逻辑层校验参数，通过Boost线程池创建独立录制任务线程；3\. 系统适配层检测当前Wayland环境，申请屏幕捕获权限；4\. 核心能力层同步启动视频帧捕获、音频采集任务；5\. 原始音视频数据送入FFmpeg编码模块进行压缩编码；6\. 编码后的帧数据进行媒体封装，实时写入本地文件；7\. 录制终止后，关闭资源、完成文件封装，回调UI层展示录制结果。

## 4\.3 线程架构设计

- **UI主线程**：Qt事件循环，负责界面交互、状态更新、快捷键监听。

- **屏幕捕获线程**：独立后台线程，循环拉取Wayland屏幕帧，输出原始RGB/YUV数据。

- **音频采集线程**：同步采集音频采样数据，保证音频时序连续性。

- **FFmpeg编码线程**：异步处理音视频编码，避免阻塞采集线程。

- **文件IO线程**：异步写入媒体文件，减少磁盘IO对录制性能的影响。

所有后台线程由Boost线程池统一管理，支持任务暂停、终止、资源回收，避免线程泄露。

# 5\. 详细实现步骤

## 5\.1 开发环境搭建

1. 搭建Linux开发环境（Ubuntu24\.04\+），安装Wayland开发库、Qt开发套件、Boost库、FFmpeg开发依赖包。

2. 配置编译环境，基于CMake构建跨平台编译脚本，配置C\+\+17编译标准，链接Qt、Boost、FFmpeg动态库。

3. 开启Wayland调试模式，适配xdg\-desktop\-portal权限机制，解决录屏权限拦截问题。

## 5\.2 基础框架开发

1. **Qt UI框架搭建**：设计主界面、参数配置界面、区域选择工具、托盘菜单，实现快捷键绑定、状态提示、文件路径配置功能。

2. **Boost任务调度框架封装**：封装Boost线程池、异步定时器、信号槽机制，实现任务的创建、暂停、终止、资源自动回收。

3. **系统环境检测模块开发**：读取系统环境变量，判断当前桌面会话为Wayland/X11，适配不同捕获方案。

## 5\.3 Wayland屏幕捕获模块实现

1. 基于xdg\-desktop\-portal统一录屏接口，向系统申请屏幕捕获权限，获取Wayland会话捕获句柄。

2. 调用wl\_shm共享内存接口，读取Wayland桌面窗口原始帧数据，支持全屏、自定义区域裁剪。

3. 帧数据格式转换，将Wayland原始ARGB帧转换为FFmpeg可处理的YUV420P格式，保证编码兼容性。

4. 按设定帧率循环捕获帧数据，通过队列缓存帧，避免帧丢失、重复采集。

## 5\.4 音频采集模块实现

1. 基于PulseAudio API，创建音频采集流，分别捕获系统回放音频、麦克风输入音频。

2. 实现双音频源混合处理，统一采样率、位深、声道格式，保证音频数据规范性。

3. 音频帧时序校准，与视频帧时间戳对齐，为音画同步提供数据支撑。

## 5\.5 FFmpeg编码与封装模块实现

1. 初始化FFmpeg编码上下文，根据用户配置初始化H\.264/H\.265/VP9编码器，配置码率、帧率、分辨率参数。

2. 实现原始视频、音频数据的编码预处理，格式转换、重采样、缓存队列管理。

3. 音视频帧编码处理，异步将原始数据编码为压缩帧，写入媒体封装上下文。

4. 实现MP4/MKV/WebM等格式封装，动态写入文件尾信息，保证录制异常退出后文件可正常播放。

5. 适配显卡硬件加速编码（VA\-API），降低CPU占用，提升高分辨率录制性能。

## 5\.6 业务逻辑与状态控制实现

1. 基于Boost信号与槽机制，实现录制开始、暂停、继续、终止的状态流转管理。

2. 实现音画同步算法，通过时间戳校准音视频帧播放时序，解决音画偏移问题。

3. 添加异常处理逻辑，捕获权限不足、磁盘已满、编码失败、线程异常等错误，弹窗提示并安全释放资源。

4. 实现日志记录功能，分级记录调试、信息、错误日志，支持日志持久化存储。

## 5\.7 功能测试与优化

1. 功能测试：全覆盖测试全屏录制、区域录制、音频录制、多格式导出、快捷键控制功能。

2. 性能测试：监测CPU、内存、磁盘IO占用，优化线程调度与帧缓存策略。

3. 兼容性测试：在不同Linux发行版、不同显卡硬件下测试适配性，修复兼容问题。

4. 稳定性测试：长时间连续录制测试，验证无崩溃、无帧丢失、音画同步稳定。

# 6\. 重点难点分析与解决方案

## 6\.1 Wayland环境录屏权限与捕获难点

### 难点问题

Wayland相较于X11采用严格的权限隔离机制，禁止应用直接读取屏幕帧数据，传统X11录屏方案无法复用；不同Wayland桌面（GNOME、KDE、Sway）私有协议存在差异，通用捕获难度大。

### 解决方案

放弃私有协议适配，采用Linux通用标准**xdg\-desktop\-portal**录屏接口，该接口为Freedesktop通用规范，兼容所有主流Wayland桌面环境；通过系统弹窗申请用户授权，合规获取屏幕捕获权限，无需绕过系统安全机制，保证软件兼容性与安全性。同时封装统一捕获接口，屏蔽不同桌面的底层差异。

## 6\.2 Wayland帧数据格式适配难点

### 难点问题

Wayland通过wl\_shm输出的屏幕帧多为ARGB格式，与FFmpeg编码所需的YUV格式不匹配，直接编码会出现花屏、色彩失真；同时帧捕获时序不稳定，易出现帧率波动、帧重复、帧丢失问题。

### 解决方案

集成高效像素格式转换算法，基于CPU快速完成ARGB转YUV420P格式转换，适配FFmpeg编码标准；设计**环形帧缓存队列**，对捕获的帧数据进行时序排序、去重、补帧处理，固定输出帧率，保证录制画面流畅稳定。

## 6\.3 音画同步难点

### 难点问题

视频帧捕获、音频采样为两个独立线程，系统调度延迟会导致音画时序偏移，出现画面快、声音滞后或声音快、画面滞后的问题，长时间录制偏移量持续累积。

### 解决方案

基于系统单调时钟统一音视频时间戳基准，所有音视频帧均绑定全局统一时间戳；设计动态同步校准机制，实时对比音视频帧时序差，当偏移量超过阈值时，自动丢弃冗余帧或补帧修复，抑制时序偏移累积，保证全程音画同步误差稳定在10ms以内。

## 6\.4 多线程异步任务调度稳定性难点

### 难点问题

录屏包含捕获、采集、编码、IO多线程并行任务，线程间资源竞争、任务阻塞、线程泄露易导致程序卡顿、崩溃、内存溢出；Qt UI线程与后台线程交互不当会引发界面卡死。

### 解决方案

基于Boost线程池统一管理所有后台任务，设置线程优先级、任务超时机制，避免任务堆积；采用**线程隔离\+消息队列通信**机制，线程间不直接共享资源，通过消息队列传递数据，解决资源竞争问题；严格区分UI线程与业务线程，所有UI更新操作通过Qt信号槽异步回调，杜绝后台线程直接操作UI控件。

## 6\.5 高分辨率录制性能瓶颈

### 难点问题

4K高分辨率屏幕录制时，原始帧数据量巨大，CPU编码压力过高，导致帧率下降、画面卡顿、系统资源占用飙升。

### 解决方案

适配Linux VA\-API硬件加速编码，调用显卡硬件编码器完成H\.264/H\.265编码，大幅降低CPU占用；动态码率自适应策略，根据系统负载自动调整编码码率和画质；优化帧缓存大小，避免内存占用过高，平衡录制画质与性能。

## 6\.6 异常场景容错难点

### 难点问题

录制过程中磁盘空间不足、权限撤销、桌面会话重启、进程后台挂起等异常场景，易导致文件损坏、程序闪退、数据丢失。

### 解决方案

增加实时磁盘空间检测、权限状态监听机制，提前预判异常并弹窗提示；优化FFmpeg文件写入逻辑，采用分段封装策略，实时写入文件关键帧数据，即使程序意外退出，已录制片段可正常播放；所有线程退出时自动释放编码上下文、文件句柄、系统资源，杜绝资源泄露。

# 7\. 系统优化策略

1. **资源优化**：通过Boost线程池复用线程，避免频繁创建销毁线程；动态调整缓存队列大小，根据分辨率、帧率自适应内存占用。

2. **编码优化**：开启FFmpeg编码预优化，启用帧预测、压缩算法优化，在画质无损前提下降低文件体积。

3. **IO优化**：采用批量写入机制，减少磁盘IO次数，提升大体积视频文件写入效率。

4. **功耗优化**：后台录制时降低线程调度频率，闲置时释放冗余资源，降低系统功耗。

# 8\. 总结与扩展方向

本项目基于C\+\+、Boost、Qt、FFmpeg技术栈，原生适配Linux Wayland环境，通过分层架构、多线程异步调度、标准协议捕获方案，解决了Wayland录屏的兼容性、稳定性、性能问题，实现了轻量化、高性能的录屏核心能力。软件架构解耦性强，具备良好的可扩展性。

后续可扩展功能：视频剪辑、水印添加、GIF动态图导出、直播推流、定时录制、录屏截图联动、云端上传等。


